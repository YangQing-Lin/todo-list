# 教学 Part 8: 软删除与回收站

> **学习阶段**: 中级 / 数据库设计模式
> **前置要求**: 已掌握基础 CRUD、数据库迁移概念
> **学习目标**: 理解软删除模式、实现回收站功能、数据库迁移实践
> **时间投入**: 3-4 小时(理解原理 + 实现 + 测试)

---

## 🎯 功能分析 - Linus 式思考

### Linus 的三个问题

**1. "这是个真问题还是臆想出来的?"**

✅ **真实问题**:
- 用户误删重要待办事项 → 数据永久丢失 → 用户愤怒
- 需要审计追踪 → 谁在什么时候删除了什么
- 法规要求 → 某些数据必须保留一段时间

**真实场景**:
```
15:00 - 用户不小心点击"删除"
15:01 - "糟糕！那个任务还有重要信息！"
15:02 - 联系客服："能恢复吗？"
15:03 - 客服："抱歉，无法恢复..."
```

**为什么需要软删除?**
- **用户友好**: 提供"后悔药"
- **数据完整性**: 保留关联数据的引用
- **审计需求**: 知道数据何时被"删除"
- **合规要求**: GDPR 等法规的数据保留要求

**2. "有更简单的方法吗?"**

💡 **核心思路**: 标记删除而非物理删除

**硬删除**(不可恢复):
```sql
DELETE FROM todos WHERE id = 1;  -- 数据永久消失
```

**软删除**(可恢复):
```sql
UPDATE todos SET deleted_at = NOW() WHERE id = 1;  -- 只是标记
-- 查询时过滤
SELECT * FROM todos WHERE deleted_at IS NULL;
```

**3. "会破坏什么吗?"**

⚠️ **潜在风险**:
- 所有查询都要加 `WHERE deleted_at IS NULL` → 容易遗漏
- 数据量膨胀 → 回收站数据越来越多
- 唯一约束问题 → 删除后无法创建同名数据

✅ **安全设计**:
- 默认过滤已删除数据
- 定期清理回收站(30天后自动永久删除)
- 考虑唯一约束的软删除兼容

---

## 📚 核心知识讲解

### 1. 软删除的设计模式

**方案 A: 布尔标记** (简单但信息少)
```sql
ALTER TABLE todos ADD COLUMN is_deleted BOOLEAN DEFAULT FALSE;
```

**方案 B: 时间戳标记** (推荐，信息更丰富)
```sql
ALTER TABLE todos ADD COLUMN deleted_at DATETIME DEFAULT NULL;
```

**为什么选择时间戳?**
- 知道"什么时候"删除的
- `NULL` 表示未删除，有值表示已删除
- 可以实现"30天后自动清理"

---

### 2. 数据库迁移

**SQLite 的限制**: 不支持 `ALTER TABLE ... DROP COLUMN`

**迁移策略**:
```go
// 检查列是否存在
func (db *DB) ensureDeletedAtColumn() error {
    rows, err := db.conn.Query(`PRAGMA table_info(todos);`)
    // ...

    if !hasDeletedAtColumn {
        _, err := db.conn.Exec(`
            ALTER TABLE todos ADD COLUMN deleted_at DATETIME DEFAULT NULL
        `)
        if err != nil {
            return err
        }
        log.Println("Added deleted_at column to todos table")
    }
    return nil
}
```

---

### 3. 查询时自动过滤

**问题**: 每次查询都要记得加条件
```go
// ❌ 容易忘记
db.Query("SELECT * FROM todos")

// ❌ 每次都要写
db.Query("SELECT * FROM todos WHERE deleted_at IS NULL")
```

**解决方案**: 在数据库层统一处理
```go
// ✅ 默认只查询未删除的
func (db *DB) ListTodos(filter TodoFilter) ([]model.Todo, int, error) {
    baseQuery := "SELECT ... FROM todos WHERE deleted_at IS NULL"
    // ...
}

// ✅ 显式查询回收站
func (db *DB) ListTrash() ([]model.Todo, error) {
    query := "SELECT ... FROM todos WHERE deleted_at IS NOT NULL"
    // ...
}
```

---

### 4. 回收站功能设计

**API 设计**:
```
DELETE /api/v1/todos/{id}           → 软删除(移入回收站)
GET    /api/v1/todos/trash          → 查看回收站
POST   /api/v1/todos/{id}/restore   → 恢复
DELETE /api/v1/todos/{id}/permanent → 永久删除
DELETE /api/v1/todos/trash          → 清空回收站
```

**数据流**:
```
正常列表                    回收站
[Todo 1]  --删除-->        [Todo 1 (deleted_at: 2024-01-01)]
[Todo 2]                   [Todo 3 (deleted_at: 2024-01-02)]
[Todo 4]  <--恢复--
                           --永久删除--> 物理删除
```

---

## 💻 完整代码示例

### 步骤 1: 更新数据模型

在 `model/todo.go` 中添加:

```go
type Todo struct {
    ID          int        `json:"id"`
    Version     int        `json:"version"`
    Title       string     `json:"title"`
    Description string     `json:"description"`
    Status      string     `json:"status"`
    DueDate     *time.Time `json:"due_date,omitempty"`
    CreatedAt   time.Time  `json:"created_at"`
    UpdatedAt   time.Time  `json:"updated_at"`
    CompletedAt *time.Time `json:"completed_at,omitempty"`
    DeletedAt   *time.Time `json:"deleted_at,omitempty"`  // 新增: 软删除时间
}

// IsDeleted 检查是否已删除
func (t *Todo) IsDeleted() bool {
    return t.DeletedAt != nil
}
```

---

### 步骤 2: 数据库迁移

在 `database/db.go` 的 `initSchema()` 中添加:

```go
func (db *DB) initSchema() error {
    schema := `
        CREATE TABLE IF NOT EXISTS todos (
            id INTEGER PRIMARY KEY AUTOINCREMENT,
            version INTEGER NOT NULL DEFAULT 1,
            title TEXT NOT NULL,
            description TEXT,
            status TEXT NOT NULL DEFAULT 'pending',
            priority INTEGER NOT NULL DEFAULT 1,
            due_date TEXT,
            created_at DATETIME NOT NULL,
            updated_at DATETIME NOT NULL,
            completed_at DATETIME,
            deleted_at DATETIME DEFAULT NULL
        );

        CREATE INDEX IF NOT EXISTS idx_status ON todos(status);
        CREATE INDEX IF NOT EXISTS idx_created_at ON todos(created_at DESC);
        CREATE INDEX IF NOT EXISTS idx_deleted_at ON todos(deleted_at);
    `

    if _, err := db.conn.Exec(schema); err != nil {
        return err
    }

    // 数据库迁移: 确保 deleted_at 列存在
    if err := db.ensureDeletedAtColumn(); err != nil {
        return err
    }

    return db.ensureVersionColumn()
}

// ensureDeletedAtColumn 确保 deleted_at 列存在(向后兼容)
func (db *DB) ensureDeletedAtColumn() error {
    rows, err := db.conn.Query(`PRAGMA table_info(todos);`)
    if err != nil {
        return fmt.Errorf("failed to inspect todos table: %w", err)
    }
    defer rows.Close()

    hasDeletedAtColumn := false
    for rows.Next() {
        var (
            cid        int
            name       string
            dataType   string
            notNull    int
            defaultVal sql.NullString
            pk         int
        )
        if err := rows.Scan(&cid, &name, &dataType, &notNull, &defaultVal, &pk); err != nil {
            return fmt.Errorf("failed to scan todos schema: %w", err)
        }
        if name == "deleted_at" {
            hasDeletedAtColumn = true
            break
        }
    }

    if err := rows.Err(); err != nil {
        return fmt.Errorf("failed to iterate todos schema: %w", err)
    }

    if hasDeletedAtColumn {
        return nil
    }

    // 添加 deleted_at 列
    alterStmt := `ALTER TABLE todos ADD COLUMN deleted_at DATETIME DEFAULT NULL`
    if _, err := db.conn.Exec(alterStmt); err != nil {
        return fmt.Errorf("failed to add deleted_at column: %w", err)
    }

    log.Println("Database migration: added deleted_at column")
    return nil
}
```

---

### 步骤 3: 数据库层 - 软删除操作

在 `database/db.go` 中添加:

```go
// SoftDeleteTodo 软删除待办事项(移入回收站)
func (db *DB) SoftDeleteTodo(id int) error {
    query := `
        UPDATE todos
        SET deleted_at = ?, updated_at = ?
        WHERE id = ? AND deleted_at IS NULL
    `

    now := time.Now()
    result, err := db.conn.Exec(query, now, now, id)
    if err != nil {
        return fmt.Errorf("软删除失败: %w", err)
    }

    rows, err := result.RowsAffected()
    if err != nil {
        return fmt.Errorf("获取影响行数失败: %w", err)
    }

    if rows == 0 {
        return fmt.Errorf("待办事项不存在或已在回收站中")
    }

    return nil
}

// RestoreTodo 从回收站恢复待办事项
func (db *DB) RestoreTodo(id int) error {
    query := `
        UPDATE todos
        SET deleted_at = NULL, updated_at = ?
        WHERE id = ? AND deleted_at IS NOT NULL
    `

    result, err := db.conn.Exec(query, time.Now(), id)
    if err != nil {
        return fmt.Errorf("恢复失败: %w", err)
    }

    rows, err := result.RowsAffected()
    if err != nil {
        return fmt.Errorf("获取影响行数失败: %w", err)
    }

    if rows == 0 {
        return fmt.Errorf("待办事项不在回收站中")
    }

    return nil
}

// PermanentDeleteTodo 永久删除待办事项
func (db *DB) PermanentDeleteTodo(id int) error {
    // 只能永久删除回收站中的项目(安全措施)
    query := `DELETE FROM todos WHERE id = ? AND deleted_at IS NOT NULL`

    result, err := db.conn.Exec(query, id)
    if err != nil {
        return fmt.Errorf("永久删除失败: %w", err)
    }

    rows, err := result.RowsAffected()
    if err != nil {
        return fmt.Errorf("获取影响行数失败: %w", err)
    }

    if rows == 0 {
        return fmt.Errorf("待办事项不在回收站中，无法永久删除")
    }

    return nil
}

// ListTrash 获取回收站列表
func (db *DB) ListTrash() ([]model.Todo, error) {
    query := `
        SELECT id, version, title, description, status, due_date,
               created_at, updated_at, completed_at, deleted_at
        FROM todos
        WHERE deleted_at IS NOT NULL
        ORDER BY deleted_at DESC
    `

    rows, err := db.conn.Query(query)
    if err != nil {
        return nil, fmt.Errorf("查询回收站失败: %w", err)
    }
    defer rows.Close()

    var todos []model.Todo
    for rows.Next() {
        var todo model.Todo
        var dueDate, completedAt, deletedAt sql.NullString

        err := rows.Scan(
            &todo.ID,
            &todo.Version,
            &todo.Title,
            &todo.Description,
            &todo.Status,
            &dueDate,
            &todo.CreatedAt,
            &todo.UpdatedAt,
            &completedAt,
            &deletedAt,
        )
        if err != nil {
            return nil, fmt.Errorf("扫描失败: %w", err)
        }

        if dueDate.Valid {
            t, _ := time.Parse(time.RFC3339, dueDate.String)
            todo.DueDate = &t
        }
        if completedAt.Valid {
            t, _ := time.Parse(time.RFC3339, completedAt.String)
            todo.CompletedAt = &t
        }
        if deletedAt.Valid {
            t, _ := time.Parse(time.RFC3339, deletedAt.String)
            todo.DeletedAt = &t
        }

        todos = append(todos, todo)
    }

    return todos, rows.Err()
}

// ClearTrash 清空回收站
func (db *DB) ClearTrash() (int, error) {
    query := `DELETE FROM todos WHERE deleted_at IS NOT NULL`

    result, err := db.conn.Exec(query)
    if err != nil {
        return 0, fmt.Errorf("清空回收站失败: %w", err)
    }

    rows, err := result.RowsAffected()
    if err != nil {
        return 0, fmt.Errorf("获取影响行数失败: %w", err)
    }

    return int(rows), nil
}

// CleanupOldTrash 清理超过指定天数的回收站数据
func (db *DB) CleanupOldTrash(days int) (int, error) {
    cutoff := time.Now().AddDate(0, 0, -days)

    query := `DELETE FROM todos WHERE deleted_at IS NOT NULL AND deleted_at < ?`

    result, err := db.conn.Exec(query, cutoff)
    if err != nil {
        return 0, fmt.Errorf("清理旧数据失败: %w", err)
    }

    rows, err := result.RowsAffected()
    if err != nil {
        return 0, fmt.Errorf("获取影响行数失败: %w", err)
    }

    return int(rows), nil
}

// GetTrashStats 获取回收站统计信息
func (db *DB) GetTrashStats() (int, error) {
    var count int
    err := db.conn.QueryRow(`SELECT COUNT(*) FROM todos WHERE deleted_at IS NOT NULL`).Scan(&count)
    if err != nil {
        return 0, fmt.Errorf("查询回收站统计失败: %w", err)
    }
    return count, nil
}
```

---

### 步骤 4: 更新现有查询(过滤已删除数据)

修改 `ListTodos` 函数:

```go
func (db *DB) ListTodos(filter TodoFilter) ([]model.Todo, int, error) {
    // ... 设置默认值 ...

    // 关键改动: 默认排除已删除的数据
    baseQuery := `SELECT id, version, title, description, status, due_date,
                         created_at, updated_at, completed_at
                  FROM todos
                  WHERE deleted_at IS NULL`  // 只查询未删除的
    args := []interface{}{}

    // ... 其余代码不变 ...
}
```

同样更新 `GetTodoByID`:

```go
func (db *DB) GetTodoByID(id int) (*model.Todo, error) {
    query := `
        SELECT id, version, title, description, status, due_date,
               created_at, updated_at, completed_at
        FROM todos
        WHERE id = ? AND deleted_at IS NULL
    `
    // ... 其余代码不变 ...
}
```

---

### 步骤 5: Handler 层实现

在 `handler/handler.go` 中添加:

```go
// DeleteTodo 删除待办事项(软删除)
func (h *Handler) DeleteTodo(w http.ResponseWriter, r *http.Request) {
    idStr := r.PathValue("id")
    id, err := strconv.Atoi(idStr)
    if err != nil || id <= 0 {
        h.sendError(w, http.StatusBadRequest, "INVALID_ID", "无效的 ID")
        return
    }

    // 使用软删除替代硬删除
    if err := h.db.SoftDeleteTodo(id); err != nil {
        log.Printf("软删除失败: %v", err)
        h.sendError(w, http.StatusInternalServerError, "DELETE_ERROR", err.Error())
        return
    }

    h.sendJSON(w, http.StatusOK, Response{
        Success: true,
        Message: "已移入回收站",
    })
}

// ListTrash 获取回收站列表
func (h *Handler) ListTrash(w http.ResponseWriter, r *http.Request) {
    todos, err := h.db.ListTrash()
    if err != nil {
        log.Printf("获取回收站失败: %v", err)
        h.sendError(w, http.StatusInternalServerError, "TRASH_ERROR", "获取回收站失败")
        return
    }

    h.sendJSON(w, http.StatusOK, Response{
        Success: true,
        Data: map[string]interface{}{
            "todos": todos,
            "count": len(todos),
        },
        Message: "获取回收站成功",
    })
}

// RestoreTodo 从回收站恢复
func (h *Handler) RestoreTodo(w http.ResponseWriter, r *http.Request) {
    idStr := r.PathValue("id")
    id, err := strconv.Atoi(idStr)
    if err != nil || id <= 0 {
        h.sendError(w, http.StatusBadRequest, "INVALID_ID", "无效的 ID")
        return
    }

    if err := h.db.RestoreTodo(id); err != nil {
        log.Printf("恢复失败: %v", err)
        h.sendError(w, http.StatusInternalServerError, "RESTORE_ERROR", err.Error())
        return
    }

    h.sendJSON(w, http.StatusOK, Response{
        Success: true,
        Message: "恢复成功",
    })
}

// PermanentDeleteTodo 永久删除
func (h *Handler) PermanentDeleteTodo(w http.ResponseWriter, r *http.Request) {
    idStr := r.PathValue("id")
    id, err := strconv.Atoi(idStr)
    if err != nil || id <= 0 {
        h.sendError(w, http.StatusBadRequest, "INVALID_ID", "无效的 ID")
        return
    }

    if err := h.db.PermanentDeleteTodo(id); err != nil {
        log.Printf("永久删除失败: %v", err)
        h.sendError(w, http.StatusInternalServerError, "DELETE_ERROR", err.Error())
        return
    }

    h.sendJSON(w, http.StatusOK, Response{
        Success: true,
        Message: "已永久删除",
    })
}

// ClearTrash 清空回收站
func (h *Handler) ClearTrash(w http.ResponseWriter, r *http.Request) {
    count, err := h.db.ClearTrash()
    if err != nil {
        log.Printf("清空回收站失败: %v", err)
        h.sendError(w, http.StatusInternalServerError, "CLEAR_ERROR", "清空回收站失败")
        return
    }

    h.sendJSON(w, http.StatusOK, Response{
        Success: true,
        Data: map[string]interface{}{
            "deleted": count,
        },
        Message: fmt.Sprintf("已永久删除 %d 条数据", count),
    })
}
```

---

### 步骤 6: 注册路由

在 `api/routes.go` 中添加:

```go
// 回收站路由(注意顺序: 具体路由要在通配符路由之前)
mux.HandleFunc("GET "+base+"/trash", withMiddlewares(h.ListTrash))
mux.HandleFunc("DELETE "+base+"/trash", withMiddlewares(h.ClearTrash))

// 单个项目的恢复和永久删除
mux.HandleFunc("POST "+base+"/{id}/restore", withMiddlewares(h.RestoreTodo))
mux.HandleFunc("DELETE "+base+"/{id}/permanent", withMiddlewares(h.PermanentDeleteTodo))

// 原有的删除路由现在变成软删除
mux.HandleFunc("DELETE "+base+"/{id}", withMiddlewares(h.DeleteTodo))
```

---

## ⚠️ 关键点解析

### 1. 向后兼容性

**问题**: 旧代码可能直接查询 `todos` 表

**解决方案**:
```go
// 所有现有查询都加上条件
WHERE deleted_at IS NULL

// 或者创建视图(数据库层面的解决方案)
CREATE VIEW active_todos AS
SELECT * FROM todos WHERE deleted_at IS NULL;
```

---

### 2. 唯一约束问题

**问题**: 如果有唯一约束(如 `title` 唯一)，软删除后无法创建同名数据

**解决方案**:
```sql
-- 方案 A: 复合唯一约束
CREATE UNIQUE INDEX idx_title_unique ON todos(title) WHERE deleted_at IS NULL;

-- 方案 B: 软删除时修改数据
UPDATE todos SET title = title || '_deleted_' || id WHERE id = 1;
```

**对于本项目**: Todo 没有唯一约束，不存在这个问题

---

### 3. 性能优化

**添加索引**:
```sql
-- 加速查询未删除数据
CREATE INDEX idx_deleted_at ON todos(deleted_at);

-- 复合索引(更高效)
CREATE INDEX idx_active_todos ON todos(deleted_at, created_at DESC);
```

---

### 4. 自动清理策略

**定时清理 30 天前的回收站数据**:
```go
// 可以在启动时或定时任务中执行
func (db *DB) CleanupOldTrash(days int) (int, error) {
    cutoff := time.Now().AddDate(0, 0, -days)
    result, err := db.conn.Exec(
        `DELETE FROM todos WHERE deleted_at IS NOT NULL AND deleted_at < ?`,
        cutoff,
    )
    // ...
}

// 使用
cleaned, _ := db.CleanupOldTrash(30)  // 清理 30 天前的数据
log.Printf("自动清理了 %d 条过期回收站数据", cleaned)
```

---

## 🧪 测试示例

### 测试脚本

```bash
#!/bin/bash

BASE_URL="http://localhost:7789/api/v1/todos"

echo "=== 1. 创建测试数据 ==="
TODO_ID=$(curl -s -X POST "$BASE_URL" \
  -H "Content-Type: application/json" \
  -d '{"title": "测试软删除"}' | jq -r '.data.id')
echo "创建了 Todo ID: $TODO_ID"

echo -e "\n=== 2. 软删除(移入回收站) ==="
curl -X DELETE "$BASE_URL/$TODO_ID"

echo -e "\n\n=== 3. 确认主列表中已不存在 ==="
curl -s "$BASE_URL" | jq '.data.todos | length'

echo -e "\n=== 4. 查看回收站 ==="
curl -s "$BASE_URL/trash" | jq '.data'

echo -e "\n=== 5. 从回收站恢复 ==="
curl -X POST "$BASE_URL/$TODO_ID/restore"

echo -e "\n\n=== 6. 确认已恢复到主列表 ==="
curl -s "$BASE_URL" | jq '.data.todos[] | select(.id == '$TODO_ID')'

echo -e "\n=== 7. 再次软删除 ==="
curl -X DELETE "$BASE_URL/$TODO_ID"

echo -e "\n\n=== 8. 永久删除 ==="
curl -X DELETE "$BASE_URL/$TODO_ID/permanent"

echo -e "\n\n=== 9. 确认回收站已清空 ==="
curl -s "$BASE_URL/trash" | jq '.data.count'

echo -e "\n=== 10. 测试清空回收站 ==="
# 先创建一些数据放入回收站
for i in {1..3}; do
  ID=$(curl -s -X POST "$BASE_URL" \
    -H "Content-Type: application/json" \
    -d "{\"title\": \"待删除 $i\"}" | jq -r '.data.id')
  curl -s -X DELETE "$BASE_URL/$ID" > /dev/null
done

echo "回收站数量:"
curl -s "$BASE_URL/trash" | jq '.data.count'

echo "清空回收站:"
curl -X DELETE "$BASE_URL/trash"

echo -e "\n清空后回收站数量:"
curl -s "$BASE_URL/trash" | jq '.data.count'
```

---

## ✅ 实现检查清单

**数据库层**:
- [ ] `deleted_at` 列迁移成功
- [ ] `SoftDeleteTodo` 实现正确
- [ ] `RestoreTodo` 实现正确
- [ ] `PermanentDeleteTodo` 实现正确
- [ ] `ListTrash` 实现正确
- [ ] `ClearTrash` 实现正确
- [ ] 现有查询已过滤已删除数据

**API 层**:
- [ ] `DELETE /todos/{id}` 变为软删除
- [ ] `GET /todos/trash` 返回回收站列表
- [ ] `POST /todos/{id}/restore` 恢复功能
- [ ] `DELETE /todos/{id}/permanent` 永久删除
- [ ] `DELETE /todos/trash` 清空回收站

**安全性**:
- [ ] 只能永久删除回收站中的数据
- [ ] 不能恢复不在回收站中的数据
- [ ] 主列表不显示已删除数据

---

## 💡 常见问题 FAQ

### Q1: 软删除会影响性能吗?

**答**: 会有轻微影响:
- 每次查询都要加 `WHERE deleted_at IS NULL`
- 表数据量会比实际活跃数据大

**优化**:
1. 添加 `deleted_at` 索引
2. 定期清理过期的回收站数据
3. 对于大表，考虑分区(活跃数据和已删除数据分开存储)

### Q2: 如何处理关联数据?

**答**: 如果 Todo 有子任务，删除时:
- 方案 A: 级联软删除(子任务也标记删除)
- 方案 B: 阻止删除(有子任务时不允许删除)
- 方案 C: 孤儿数据(子任务保留，显示"父任务已删除")

### Q3: 如何实现"撤销"功能?

**答**: 软删除天然支持撤销:
```go
// 删除后立即显示"撤销"按钮
// 点击撤销调用 restore 接口
POST /api/v1/todos/{id}/restore
```

### Q4: GDPR 数据删除怎么处理?

**答**: GDPR 要求"被遗忘权"，需要真正删除:
```go
// 提供真正的数据清除接口(仅限管理员)
func (db *DB) GDPRDelete(userID int) error {
    // 物理删除该用户的所有数据
    _, err := db.conn.Exec("DELETE FROM todos WHERE user_id = ?", userID)
    return err
}
```

---

## 🚀 实现步骤建议

**第 1 步**: 添加 `ensureDeletedAtColumn` 迁移函数

**第 2 步**: 更新 `model.Todo` 添加 `DeletedAt` 字段

**第 3 步**: 实现数据库层的软删除操作

**第 4 步**: 修改现有查询，添加 `WHERE deleted_at IS NULL`

**第 5 步**: 实现 Handler 层的回收站功能

**第 6 步**: 注册路由

**第 7 步**: 测试所有场景

---

**现在，开始实现软删除与回收站功能吧！记住：标记删除、默认过滤、提供恢复。** 🚀
