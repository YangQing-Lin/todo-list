# 教学 Part 7: 导入/导出功能

> **学习阶段**: 中级 / Go I/O 编程
> **前置要求**: 已掌握基础 CRUD、HTTP 处理
> **学习目标**: 掌握 `encoding/csv`、`encoding/json`、`io` 包、HTTP 文件下载/上传
> **时间投入**: 3-4 小时(理解原理 + 实现 + 测试)

---

## 🎯 功能分析 - Linus 式思考

### Linus 的三个问题

**1. "这是个真问题还是臆想出来的?"**

✅ **真实问题**:
- 用户想备份自己的待办事项数据
- 用户从其他工具迁移数据过来
- 团队间共享任务清单
- 数据分析需要导出到 Excel

**为什么需要导入导出?**
- **数据可移植性**: 用户拥有自己的数据
- **系统集成**: 与其他工具互通
- **备份恢复**: 防止数据丢失
- **批量操作**: 一次性导入大量数据

**2. "有更简单的方法吗?"**

💡 **核心思路**: 流式处理 + 标准格式

**错误方法**(全部加载到内存):
```go
// ❌ 10万条数据会占用大量内存
todos := db.GetAllTodos()
jsonData, _ := json.Marshal(todos)
w.Write(jsonData)
```

**正确方法**(流式写入):
```go
// ✅ 边查询边写入，内存占用恒定
encoder := json.NewEncoder(w)
for _, todo := range todos {
    encoder.Encode(todo)
}
```

**3. "会破坏什么吗?"**

⚠️ **潜在风险**:
- 导入恶意数据(SQL 注入、XSS)
- 大文件导入导致内存溢出
- 并发导入导致数据不一致

✅ **安全设计**:
- 输入验证和数据清洗
- 文件大小限制
- 事务保证原子性

---

## 📚 核心知识讲解

### 1. Go 的 I/O 模型

Go 的 I/O 设计基于两个核心接口:

```go
// io.Reader - 读取数据的抽象
type Reader interface {
    Read(p []byte) (n int, err error)
}

// io.Writer - 写入数据的抽象
type Writer interface {
    Write(p []byte) (n int, err error)
}
```

**为什么这个设计很优雅?**
- `http.ResponseWriter` 实现了 `io.Writer`
- `http.Request.Body` 实现了 `io.Reader`
- 可以直接把数据"流"到 HTTP 响应中

```go
// 直接把 CSV 流到 HTTP 响应
csvWriter := csv.NewWriter(w)  // w 是 http.ResponseWriter
csvWriter.Write([]string{"ID", "Title"})
csvWriter.Flush()
```

---

### 2. encoding/csv 包

**CSV 写入**:
```go
import "encoding/csv"

func writeCSV(w io.Writer, todos []model.Todo) error {
    writer := csv.NewWriter(w)
    defer writer.Flush()

    // 写入表头
    if err := writer.Write([]string{"ID", "标题", "状态"}); err != nil {
        return err
    }

    // 写入数据行
    for _, todo := range todos {
        row := []string{
            strconv.Itoa(todo.ID),
            todo.Title,
            todo.Status,
        }
        if err := writer.Write(row); err != nil {
            return err
        }
    }

    return writer.Error()  // 检查缓冲区错误
}
```

**CSV 读取**:
```go
func readCSV(r io.Reader) ([]model.Todo, error) {
    reader := csv.NewReader(r)

    // 跳过表头
    if _, err := reader.Read(); err != nil {
        return nil, err
    }

    var todos []model.Todo
    for {
        record, err := reader.Read()
        if err == io.EOF {
            break
        }
        if err != nil {
            return nil, err
        }

        id, _ := strconv.Atoi(record[0])
        todos = append(todos, model.Todo{
            ID:     id,
            Title:  record[1],
            Status: record[2],
        })
    }

    return todos, nil
}
```

**关键点**:
- `csv.Writer` 有内部缓冲区，必须调用 `Flush()`
- `csv.Reader.Read()` 返回 `io.EOF` 表示文件结束
- CSV 默认用逗号分隔，可以通过 `writer.Comma = '\t'` 改为 Tab

---

### 3. encoding/json 流式处理

**JSON 数组导出**(流式):
```go
func exportJSON(w io.Writer, todos []model.Todo) error {
    w.Write([]byte("[\n"))

    encoder := json.NewEncoder(w)
    encoder.SetIndent("", "  ")

    for i, todo := range todos {
        if i > 0 {
            w.Write([]byte(",\n"))
        }
        if err := encoder.Encode(todo); err != nil {
            return err
        }
    }

    w.Write([]byte("]\n"))
    return nil
}
```

**JSON 数组导入**(流式):
```go
func importJSON(r io.Reader) ([]model.Todo, error) {
    decoder := json.NewDecoder(r)

    // 读取开始的 '['
    if _, err := decoder.Token(); err != nil {
        return nil, err
    }

    var todos []model.Todo
    for decoder.More() {
        var todo model.Todo
        if err := decoder.Decode(&todo); err != nil {
            return nil, err
        }
        todos = append(todos, todo)
    }

    return todos, nil
}
```

---

### 4. HTTP 文件下载

**设置响应头触发下载**:
```go
func (h *Handler) ExportTodos(w http.ResponseWriter, r *http.Request) {
    format := r.URL.Query().Get("format")

    switch format {
    case "csv":
        // 告诉浏览器这是一个要下载的文件
        w.Header().Set("Content-Type", "text/csv; charset=utf-8")
        w.Header().Set("Content-Disposition", "attachment; filename=todos.csv")
        // 添加 UTF-8 BOM，让 Excel 正确识别中文
        w.Write([]byte{0xEF, 0xBB, 0xBF})

    case "json":
        w.Header().Set("Content-Type", "application/json; charset=utf-8")
        w.Header().Set("Content-Disposition", "attachment; filename=todos.json")
    }

    // ... 写入数据
}
```

**关键响应头**:
- `Content-Type`: 告诉浏览器文件类型
- `Content-Disposition: attachment`: 触发下载而不是在浏览器显示
- `filename=xxx`: 默认的下载文件名

---

### 5. HTTP 文件上传

**处理 multipart/form-data**:
```go
func (h *Handler) ImportTodos(w http.ResponseWriter, r *http.Request) {
    // 限制上传大小(10MB)
    r.Body = http.MaxBytesReader(w, r.Body, 10<<20)

    // 解析 multipart 表单
    if err := r.ParseMultipartForm(10 << 20); err != nil {
        h.sendError(w, http.StatusBadRequest, "FILE_TOO_LARGE", "文件太大")
        return
    }

    // 获取上传的文件
    file, header, err := r.FormFile("file")
    if err != nil {
        h.sendError(w, http.StatusBadRequest, "NO_FILE", "请选择文件")
        return
    }
    defer file.Close()

    // 检查文件类型
    if !strings.HasSuffix(header.Filename, ".json") {
        h.sendError(w, http.StatusBadRequest, "INVALID_FORMAT", "只支持 JSON 文件")
        return
    }

    // 解析文件内容
    todos, err := parseImportFile(file)
    // ...
}
```

---

## 💻 完整代码示例

### 步骤 1: 数据库层 - 批量创建

在 `database/db.go` 中添加:

```go
// ImportTodos 批量导入待办事项(事务保证)
func (db *DB) ImportTodos(todos []model.Todo) (int, error) {
    if len(todos) == 0 {
        return 0, nil
    }

    // 限制单次导入数量
    if len(todos) > 1000 {
        return 0, fmt.Errorf("单次导入最多 1000 条，当前: %d", len(todos))
    }

    tx, err := db.conn.Begin()
    if err != nil {
        return 0, fmt.Errorf("开启事务失败: %w", err)
    }

    defer func() {
        if err != nil {
            tx.Rollback()
        }
    }()

    stmt, err := tx.Prepare(`
        INSERT INTO todos (title, description, status, due_date, created_at, updated_at, version)
        VALUES (?, ?, ?, ?, ?, ?, 1)
    `)
    if err != nil {
        return 0, fmt.Errorf("准备语句失败: %w", err)
    }
    defer stmt.Close()

    now := time.Now()
    imported := 0

    for _, todo := range todos {
        // 数据验证
        if todo.Title == "" {
            continue  // 跳过无效数据
        }

        // 设置默认值
        if todo.Status == "" {
            todo.Status = "pending"
        }
        if todo.CreatedAt.IsZero() {
            todo.CreatedAt = now
        }
        todo.UpdatedAt = now

        _, err = stmt.Exec(
            todo.Title,
            todo.Description,
            todo.Status,
            todo.DueDate,
            todo.CreatedAt,
            todo.UpdatedAt,
        )
        if err != nil {
            return imported, fmt.Errorf("插入第 %d 条失败: %w", imported+1, err)
        }
        imported++
    }

    if err = tx.Commit(); err != nil {
        return 0, fmt.Errorf("提交事务失败: %w", err)
    }

    return imported, nil
}

// ExportTodos 导出所有待办事项(用于导出功能)
func (db *DB) ExportTodos() ([]model.Todo, error) {
    query := `
        SELECT id, version, title, description, status, due_date,
               created_at, updated_at, completed_at
        FROM todos
        ORDER BY created_at DESC
    `

    rows, err := db.conn.Query(query)
    if err != nil {
        return nil, fmt.Errorf("查询失败: %w", err)
    }
    defer rows.Close()

    var todos []model.Todo
    for rows.Next() {
        var todo model.Todo
        var dueDate, completedAt sql.NullString

        err := rows.Scan(
            &todo.ID,
            &todo.Version,
            &todo.Title,
            &todo.Description,
            &todo.Status,
            &dueDate,
            &todo.CreatedAt,
            &todo.UpdatedAt,
            &completedAt,
        )
        if err != nil {
            return nil, fmt.Errorf("扫描失败: %w", err)
        }

        if dueDate.Valid {
            t, _ := time.Parse(time.RFC3339, dueDate.String)
            todo.DueDate = &t
        }
        if completedAt.Valid {
            t, _ := time.Parse(time.RFC3339, completedAt.String)
            todo.CompletedAt = &t
        }

        todos = append(todos, todo)
    }

    return todos, rows.Err()
}
```

---

### 步骤 2: Handler 层 - 导出功能

在 `handler/handler.go` 中添加:

```go
import (
    "encoding/csv"
    "encoding/json"
    "strconv"
    "strings"
)

// ExportTodos 导出待办事项
func (h *Handler) ExportTodos(w http.ResponseWriter, r *http.Request) {
    format := r.URL.Query().Get("format")
    if format == "" {
        format = "json"
    }

    // 获取所有数据
    todos, err := h.db.ExportTodos()
    if err != nil {
        log.Printf("导出失败: %v", err)
        h.sendError(w, http.StatusInternalServerError, "EXPORT_ERROR", "导出失败")
        return
    }

    switch format {
    case "csv":
        h.exportCSV(w, todos)
    case "json":
        h.exportJSON(w, todos)
    default:
        h.sendError(w, http.StatusBadRequest, "INVALID_FORMAT", "不支持的格式，请使用 json 或 csv")
    }
}

// exportCSV 导出为 CSV 格式
func (h *Handler) exportCSV(w http.ResponseWriter, todos []model.Todo) {
    // 设置响应头
    w.Header().Set("Content-Type", "text/csv; charset=utf-8")
    w.Header().Set("Content-Disposition", "attachment; filename=todos.csv")

    // UTF-8 BOM，让 Excel 正确识别中文
    w.Write([]byte{0xEF, 0xBB, 0xBF})

    writer := csv.NewWriter(w)
    defer writer.Flush()

    // 写入表头
    headers := []string{"ID", "标题", "描述", "状态", "截止日期", "创建时间", "完成时间"}
    if err := writer.Write(headers); err != nil {
        log.Printf("写入 CSV 表头失败: %v", err)
        return
    }

    // 写入数据
    for _, todo := range todos {
        row := []string{
            strconv.Itoa(todo.ID),
            todo.Title,
            todo.Description,
            todo.Status,
            formatTimePtr(todo.DueDate),
            todo.CreatedAt.Format("2006-01-02 15:04:05"),
            formatTimePtr(todo.CompletedAt),
        }
        if err := writer.Write(row); err != nil {
            log.Printf("写入 CSV 行失败: %v", err)
            return
        }
    }
}

// exportJSON 导出为 JSON 格式
func (h *Handler) exportJSON(w http.ResponseWriter, todos []model.Todo) {
    w.Header().Set("Content-Type", "application/json; charset=utf-8")
    w.Header().Set("Content-Disposition", "attachment; filename=todos.json")

    // 格式化输出，方便阅读
    encoder := json.NewEncoder(w)
    encoder.SetIndent("", "  ")

    if err := encoder.Encode(todos); err != nil {
        log.Printf("写入 JSON 失败: %v", err)
    }
}

// formatTimePtr 格式化时间指针
func formatTimePtr(t *time.Time) string {
    if t == nil {
        return ""
    }
    return t.Format("2006-01-02 15:04:05")
}
```

---

### 步骤 3: Handler 层 - 导入功能

```go
// ImportRequest 导入请求体(JSON 格式)
type ImportRequest struct {
    Todos []ImportTodoItem `json:"todos"`
}

// ImportTodoItem 导入的单个待办事项
type ImportTodoItem struct {
    Title       string  `json:"title"`
    Description string  `json:"description"`
    Status      string  `json:"status"`
    DueDate     *string `json:"due_date"`
}

// ImportTodos 导入待办事项
func (h *Handler) ImportTodos(w http.ResponseWriter, r *http.Request) {
    // 限制请求体大小
    r.Body = http.MaxBytesReader(w, r.Body, 10<<20) // 10MB

    contentType := r.Header.Get("Content-Type")

    var todos []model.Todo
    var err error

    if strings.HasPrefix(contentType, "multipart/form-data") {
        // 文件上传方式
        todos, err = h.parseImportFile(r)
    } else {
        // JSON 请求体方式
        todos, err = h.parseImportJSON(r)
    }

    if err != nil {
        h.sendError(w, http.StatusBadRequest, "PARSE_ERROR", err.Error())
        return
    }

    if len(todos) == 0 {
        h.sendError(w, http.StatusBadRequest, "EMPTY_DATA", "没有可导入的数据")
        return
    }

    // 执行导入
    imported, err := h.db.ImportTodos(todos)
    if err != nil {
        log.Printf("导入失败: %v", err)
        h.sendError(w, http.StatusInternalServerError, "IMPORT_ERROR", err.Error())
        return
    }

    h.sendJSON(w, http.StatusOK, Response{
        Success: true,
        Data: map[string]interface{}{
            "imported": imported,
            "total":    len(todos),
        },
        Message: fmt.Sprintf("成功导入 %d 条待办事项", imported),
    })
}

// parseImportJSON 解析 JSON 请求体
func (h *Handler) parseImportJSON(r *http.Request) ([]model.Todo, error) {
    var req ImportRequest
    if err := json.NewDecoder(r.Body).Decode(&req); err != nil {
        return nil, fmt.Errorf("JSON 解析失败: %w", err)
    }

    todos := make([]model.Todo, 0, len(req.Todos))
    for _, item := range req.Todos {
        todo := model.Todo{
            Title:       strings.TrimSpace(item.Title),
            Description: strings.TrimSpace(item.Description),
            Status:      item.Status,
        }

        // 解析截止日期
        if item.DueDate != nil && *item.DueDate != "" {
            if t, err := time.Parse("2006-01-02", *item.DueDate); err == nil {
                todo.DueDate = &t
            } else if t, err := time.Parse(time.RFC3339, *item.DueDate); err == nil {
                todo.DueDate = &t
            }
        }

        todos = append(todos, todo)
    }

    return todos, nil
}

// parseImportFile 解析上传的文件
func (h *Handler) parseImportFile(r *http.Request) ([]model.Todo, error) {
    if err := r.ParseMultipartForm(10 << 20); err != nil {
        return nil, fmt.Errorf("解析表单失败: %w", err)
    }

    file, header, err := r.FormFile("file")
    if err != nil {
        return nil, fmt.Errorf("获取文件失败: %w", err)
    }
    defer file.Close()

    filename := strings.ToLower(header.Filename)

    if strings.HasSuffix(filename, ".json") {
        return h.parseJSONFile(file)
    } else if strings.HasSuffix(filename, ".csv") {
        return h.parseCSVFile(file)
    }

    return nil, fmt.Errorf("不支持的文件格式，请使用 .json 或 .csv")
}

// parseJSONFile 解析 JSON 文件
func (h *Handler) parseJSONFile(file io.Reader) ([]model.Todo, error) {
    var todos []model.Todo
    if err := json.NewDecoder(file).Decode(&todos); err != nil {
        return nil, fmt.Errorf("JSON 文件解析失败: %w", err)
    }
    return todos, nil
}

// parseCSVFile 解析 CSV 文件
func (h *Handler) parseCSVFile(file io.Reader) ([]model.Todo, error) {
    reader := csv.NewReader(file)

    // 读取表头
    headers, err := reader.Read()
    if err != nil {
        return nil, fmt.Errorf("读取 CSV 表头失败: %w", err)
    }

    // 建立列名到索引的映射
    colIndex := make(map[string]int)
    for i, h := range headers {
        colIndex[strings.ToLower(strings.TrimSpace(h))] = i
    }

    // 检查必需列
    titleIdx, hasTitleCol := colIndex["标题"]
    if !hasTitleCol {
        titleIdx, hasTitleCol = colIndex["title"]
    }
    if !hasTitleCol {
        return nil, fmt.Errorf("CSV 缺少标题列")
    }

    var todos []model.Todo
    lineNum := 1

    for {
        lineNum++
        record, err := reader.Read()
        if err == io.EOF {
            break
        }
        if err != nil {
            return nil, fmt.Errorf("读取第 %d 行失败: %w", lineNum, err)
        }

        todo := model.Todo{
            Title: strings.TrimSpace(record[titleIdx]),
        }

        // 可选列
        if idx, ok := colIndex["描述"]; ok && idx < len(record) {
            todo.Description = strings.TrimSpace(record[idx])
        } else if idx, ok := colIndex["description"]; ok && idx < len(record) {
            todo.Description = strings.TrimSpace(record[idx])
        }

        if idx, ok := colIndex["状态"]; ok && idx < len(record) {
            todo.Status = strings.TrimSpace(record[idx])
        } else if idx, ok := colIndex["status"]; ok && idx < len(record) {
            todo.Status = strings.TrimSpace(record[idx])
        }

        todos = append(todos, todo)
    }

    return todos, nil
}
```

---

### 步骤 4: 注册路由

在 `api/routes.go` 中添加:

```go
// 导入导出路由
mux.HandleFunc("GET "+base+"/export", withMiddlewares(h.ExportTodos))
mux.HandleFunc("POST "+base+"/import", withMiddlewares(h.ImportTodos))
```

---

## ⚠️ 关键点解析

### 1. UTF-8 BOM 问题

**问题**: Excel 打开 CSV 时中文乱码

**原因**: Excel 默认使用系统编码(GBK)打开 CSV

**解决**: 在文件开头添加 UTF-8 BOM
```go
// UTF-8 BOM: 0xEF 0xBB 0xBF
w.Write([]byte{0xEF, 0xBB, 0xBF})
```

---

### 2. 流式处理 vs 全量加载

**小数据量**(< 1000 条): 全量加载没问题
```go
todos, _ := db.GetAll()
json.Marshal(todos)
```

**大数据量**(> 10000 条): 必须流式处理
```go
// 边查询边写入，内存占用 O(1)
rows, _ := db.Query("SELECT ...")
for rows.Next() {
    todo := scanTodo(rows)
    encoder.Encode(todo)  // 立即写入响应
}
```

---

### 3. 安全性考虑

**输入验证**:
```go
// 限制文件大小
r.Body = http.MaxBytesReader(w, r.Body, 10<<20)

// 验证文件类型
if !strings.HasSuffix(filename, ".json") {
    return error
}

// 清洗数据
title := strings.TrimSpace(input.Title)
if len(title) > 500 {
    title = title[:500]  // 截断
}
```

**防止 SQL 注入**: 使用参数化查询(已有)

---

### 4. 事务保证原子性

```go
// 导入要么全部成功，要么全部失败
tx, _ := db.Begin()
for _, todo := range todos {
    if err := insert(tx, todo); err != nil {
        tx.Rollback()  // 回滚所有
        return err
    }
}
tx.Commit()
```

---

## 🧪 测试示例

### 测试脚本

```bash
#!/bin/bash

BASE_URL="http://localhost:7789/api/v1/todos"

echo "=== 1. 创建测试数据 ==="
for i in {1..5}; do
  curl -s -X POST "$BASE_URL" \
    -H "Content-Type: application/json" \
    -d "{\"title\": \"测试任务 $i\", \"description\": \"描述 $i\"}" > /dev/null
done
echo "创建了 5 条测试数据"

echo -e "\n=== 2. 测试 JSON 导出 ==="
curl -s "$BASE_URL/export?format=json" -o todos.json
echo "导出到 todos.json，内容:"
head -20 todos.json

echo -e "\n=== 3. 测试 CSV 导出 ==="
curl -s "$BASE_URL/export?format=csv" -o todos.csv
echo "导出到 todos.csv，内容:"
cat todos.csv

echo -e "\n=== 4. 测试 JSON 导入 ==="
curl -X POST "$BASE_URL/import" \
  -H "Content-Type: application/json" \
  -d '{
    "todos": [
      {"title": "导入任务 1", "description": "通过 API 导入"},
      {"title": "导入任务 2", "status": "completed"},
      {"title": "导入任务 3", "due_date": "2024-12-31"}
    ]
  }'

echo -e "\n\n=== 5. 测试文件上传导入 ==="
curl -X POST "$BASE_URL/import" \
  -F "file=@todos.json"

echo -e "\n\n=== 6. 测试错误情况 ==="
# 空数据
curl -X POST "$BASE_URL/import" \
  -H "Content-Type: application/json" \
  -d '{"todos": []}'

# 无效格式
curl -s "$BASE_URL/export?format=xml"
```

---

## ✅ 实现检查清单

**导出功能**:
- [ ] JSON 格式导出正常
- [ ] CSV 格式导出正常
- [ ] CSV 在 Excel 中打开中文不乱码
- [ ] Content-Disposition 触发下载
- [ ] 空数据时返回空数组/空文件

**导入功能**:
- [ ] JSON 请求体导入正常
- [ ] JSON 文件上传导入正常
- [ ] CSV 文件上传导入正常
- [ ] 空数据时返回友好提示
- [ ] 超大文件被拒绝(10MB 限制)
- [ ] 无效格式返回错误
- [ ] 事务保证原子性

**安全性**:
- [ ] 文件大小限制生效
- [ ] 数据验证(标题非空等)
- [ ] 使用参数化查询

---

## 💡 常见问题 FAQ

### Q1: 为什么 CSV 要用 BOM?

**答**: Excel 打开 CSV 时默认使用系统编码(Windows 是 GBK)。添加 UTF-8 BOM (0xEF 0xBB 0xBF) 后，Excel 会识别为 UTF-8 编码。

### Q2: 导入大量数据时应该怎么优化?

**答**:
1. **批量插入**: 使用 `INSERT INTO ... VALUES (...), (...), (...)` 减少往返次数
2. **事务**: 一个事务内完成所有插入
3. **禁用索引**: 大量插入前禁用索引，插入后重建
4. **分批处理**: 每 1000 条提交一次，防止事务过大

### Q3: 如何支持更多格式(Excel)?

**答**: 使用第三方库:
- Excel: `github.com/xuri/excelize/v2`
- YAML: `gopkg.in/yaml.v3`

但对于学习项目，JSON 和 CSV 已经足够。

### Q4: 导出大量数据时如何显示进度?

**答**: HTTP 不适合显示进度。可以:
1. 异步导出 + 轮询状态
2. 使用 Server-Sent Events
3. 分页导出

---

## 🚀 实现步骤建议

**第 1 步**: 实现 `database/db.go` 中的 `ImportTodos` 和 `ExportTodos`

**第 2 步**: 实现 `handler/handler.go` 中的导出功能

**第 3 步**: 实现导入功能(先 JSON，后 CSV)

**第 4 步**: 注册路由

**第 5 步**: 使用测试脚本验证

---

**现在，开始实现导入导出功能吧！记住：流式处理、事务保证、安全验证。** 🚀
